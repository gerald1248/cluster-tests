for namespace in ${USER_NAMESPACES}; do
  resourcequota=`kubectl get resourcequota -n ${namespace} 2>/dev/null | wc -l`
  if [ "${resourcequota}" -eq "0" ]; then
    echo "resourcequota not set in namespace ${namespace}"
    exit 1
  fi
done
